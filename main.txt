we ask user, what time they want to cycle at
    local time
    we also capture day when they ask, in the form of local date
    we take the local time and add it to the local date
    we also take the localdatetime when they ask
    we compare those two values
        if current time > user time
            we take user time, add 24 hr, subtract that time and current time, and initiate a countdown of that time
        if current time < user time
            we subtract user time and current time, and start a countdown of that time
    
on cycle, we reset the timer by 24 hr

if program closes, we take final time
    upon reopening, we check if reopen time > cycle time
        if true
            we calculate days to skip
            re calculate sleep time
        if false
            we recalculate the sleep time

so we use executer when system is running
and we use time difference when it closes

